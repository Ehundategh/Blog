# 晨汐散余香

## 题意简述

有 $n$ 个物品，第 $i$ 个物品价值为 $w_i$，最晚在第 $t_i$ 天交易。每天至多交易一个物品。对每次询问 $p_i$，求最多使用 $p_i$ 个交易日时可以得到的最大总价值。

## Hint

如果只问一次固定天数，价值大的物品应优先考虑，并尽量安排到不晚于截止时间的最晚空闲日。

??? note "提示"
    把物品按价值从大到小排序。若当前物品还能放进某个不超过 $t_i$ 的空闲日，则选它不会劣于把这个位置留给价值更小的物品。

## 解法

将所有物品按价值从大到小排序。使用并查集维护每个日期左侧最近的空闲位置：若要放入一个截止时间为 $t_i$ 的物品，就查询 $\operatorname{Find}(t_i)$。若得到的位置非零，则把该物品安排在此处，并将这个位置与左侧位置合并。

按这个过程依次选到第 $k$ 个物品时，得到的前缀收益就是最多使用 $k$ 个交易日的最优值。所有询问只需要输出对应前缀即可。

交换证明如下：若某个最优方案中存在一个未选的高价值物品 $A$ 和一个已选的低价值物品 $B$，且 $A$ 可以被放入当前可行日集合中，那么用 $A$ 替换 $B$ 不会降低可行性，收益只会增加。因此从高价值到低价值贪心是合法的。

## 复杂度

排序复杂度为 $\mathcal{O}(n\log n)$，并查集均摊近似 $\mathcal{O}(n)$，回答询问为 $\mathcal{O}(q)$。

## 参考代码

[aroma.cpp](/Blog/assets/solution/jiuguang-summer/day7/std/aroma.cpp)
